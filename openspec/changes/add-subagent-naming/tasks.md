## 1. Policy and Contract

- [x] 1.1 Define the canonical `<purpose>-<model>-<effort>` algorithm, validation rules, retry behavior, and native-field fallback in the bundled Skill and routing policy.
- [x] 1.2 Add the standard-library naming helper and worker-name requirements to controller hook context, the worker packet template, and the worker result contract.

## 2. Documentation and Tests

- [x] 2.1 Document user-visible naming behavior and the native worker-card limitation in plugin and repository documentation.
- [x] 2.2 Add regression tests for controller naming instructions, worker result echo, normalization examples, validation, and fallback language.

## 3. Validation

- [x] 3.1 Run the offline plugin validator, unit tests, strict OpenSpec validation, and whitespace checks.
- [x] 3.2 Review the final diff for scoped changes and confirm that no unsupported dispatch field or UI guarantee was introduced.
